# Prompt — Phase 09: DevOps (Reference Only)

```
phase 09 เป็น reference สำหรับเรียนรู้ — ไม่ต้อง implement จริง

อ่าน spec/2026-07-18_core/09_DEVOPS.md เพื่อศึกษา:
  - Nginx reverse proxy config
  - CORS strategy (allow all in Express, filter at Nginx)
  - Rate limiting at edge
  - Security headers (CSP, HSTS, X-Frame)
  - TLS/HTTPS termination
  - Static file serving
  - Health checks

ไม่ต้อง implement ในโปรเจกต์ study นี้ — docker compose รัน backend โดยตรงที่ port 3000 ก็พอ
```
