# Prompt — Phase 08: E2E Tests + Final Polish

```
implement phase 08 ตาม spec/2026-07-18_core/08_E2E.md

สิ่งที่ต้องทำ:
  playwright E2E tests (5 scenarios):
    1. Login → Dashboard → Logout
    2. Customer CRUD (create → search → view)
    3. Invoice workflow (create → check stock deducted)
    4. AI Chat (ask today sales → get response)
    5. Dashboard (verify data after operations)

  final polish:
    npm run lint — no errors
    npm run typecheck — no errors
    npm test — all pass
    npx playwright test — all pass
    coverage report: backend ≥ 80%, frontend ≥ 70%
```
